The appropriate conguration settings files should be placed in this directory.
    - local.settings.php for local development
    - stage.settings.php for staging environment
    - etc.
    
Ideally, this is done during build, but if not, the files should exist inside the project's root /config directory.