
  <div{{ attributes.addClass(classes) }}>
      <span>{{ number }} {{ weight }} {{ volume }} {{ denomination }} {{ ingredient }} {% if note[0] is not empty %} ({{ note|render|striptags('<!-- -->')|trim }}){% endif %}</span>
  </div>
